import handleLanguage from './handle-language';
import promptLanguage from './prompt-language';
import handleGreeting from './handle-greeting';

export default {
  promptLanguage,
  handleLanguage,
  handleGreeting,
};
